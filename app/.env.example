# Copy to .env and keep this file free of secrets.
# Allowed values: development, production.
# Do not use VITE_* for secrets; Vite exposes VITE_* values to the client bundle.
NODORA_CONFIG_ENV=development
